# CLAUDE.md — 에넥스 오피스 홍보 사이트

이 저장소를 작업할 때 Claude Code가 항상 참고해야 하는 컨텍스트.

## 프로젝트 개요

- 목적: 명함 QR → 접속하는 오피스 가구 영업 홍보 사이트 (단일 페이지, 10섹션)
- 배포: GitHub Pages, 정적 사이트 (빌드 도구 없음)
- 주소: https://enex-office.github.io/enex-office/
- 파일 구성:
  ```
  index.html   ← 전체 (HTML+CSS+JS 한 파일)
  images/      ← 사진 (JPG, 가로 1600px 내외)
  fonts/       ← Paperlogy woff2 5웨이트 (셀프호스팅)
  ```

## 절대 원칙

1. **프레임워크·빌드 도구를 도입하지 않는다.** React/Vue/번들러 제안 금지 — 순수 HTML/CSS/JS 유지가 이 프로젝트의 설계 의도. GitHub Pages에 파일 그대로 올리는 방식이 핵심 요구사항.
2. **구글시트 연동 구조를 깨지 않는다.** 담당자·납품실적·납품사례·링크폼·문구 5개 데이터는 구글시트에서 사장님이 직접 관리한다. 이 데이터를 HTML에 하드코딩하는 방향으로 "개선"하지 말 것.
3. **한 파일 유지.** index.html을 여러 파일로 쪼개는 리팩토링 제안 금지 (배포 단순성이 목적).
4. 사용자는 비개발자다. 설명 없이 기술 용어만 나열하지 말고, 수정 후 "무엇이 바뀌었는지 + 확인 방법"을 항상 알려준다.

## 데이터 아키텍처 (중요 — 반드시 이해하고 시작)

```js
const SHEET = {
  contacts: "https://docs.google.com/.../pub?...output=csv",  // 담당자
  records:  "...",  // 납품실적 (익명 표기)
  cases:    "...",  // 납품사례 (갤러리)
  links:    "...",  // 폼/전화/카탈로그 링크
  copy:     "...",  // 문구 (커버 제목 등 12개 키)
};
```

- 각 주소는 구글시트 "파일 → 공유 → 웹에 게시 → CSV" 로 생성된 것. **주소를 임의로 바꾸거나 재발급하지 말 것** — 시트 쪽에서 재게시해야 바뀜.
- `FALLBACK` 객체: 시트 연결 실패 시 표시되는 내장 기본값. 시트 구조를 변경하면 FALLBACK도 같이 맞출 것 (샘플 데이터 역할 겸 안전망).
- CSV 파서(`parseCSV`, `toObjects`)는 자체 구현 (따옴표/쉼표 이스케이프, 시트 안내문 행 자동 제외 `※` 접두어). 라이브러리로 교체 금지.
- 시트 데이터의 열 이름(한글)이 코드 내 오브젝트 키와 매칭됨 — 예: `사진파일명`, `역할`, `추가사진`, `설계3D`. 시트 헤더를 바꾸려면 코드도 같이 바꿔야 함.
- `cases` 탭의 **`설계3D`** 열(2026-09-03 추가): 3D 설계 렌더링 파일명. 값이 있으면 그 사례가
  "3D 설계안 ↔ 실제 시공" 비교 슬라이더로 렌더됨(`render3D`). 값이 없으면 기존 갤러리 카드만.
  채워진 사례가 0건이면 섹션 전체를 숨김. **사용자가 시트만으로 세트를 늘릴 수 있어야 한다** —
  파일명을 HTML에 하드코딩하는 방식으로 되돌리지 말 것.

## 섹션 구조 (11개, id 기준)

`#top`(커버) → `#about` → `#infra` → `#business` → `#process` → `#faq`(자주 겪는 불편, 2026-08 추가) → `#records` → `#cases`(갤러리+팝업) → `#products` → `#quality` → `#contact`

각 섹션 제목·서브 문구는 `data-ck="키이름"` 속성으로 마킹되어 있고, `copy` 시트에서 텍스트를 덮어씀 (renderCopy 함수). 문구 텍스트를 코드에서 직접 고치면 시트보다 우선순위가 낮아 사용자가 시트에서 다시 덮어쓸 수 있음 — 이건 의도된 동작.

## 이미 구현된 기능 (재구현·중복 금지)

- 반응형: 모바일 세로 스크롤 / PC 일반 스크롤 (스크롤 스냅 의도적으로 제거함 — 재도입 금지, 사용자가 "정신사납다"고 명시적으로 뺀 기능)
- 모바일 하단 고정 전화바 (`.callbar`) — PC(1024px 이상)에서는 숨김
- 커버 배경 슬로우 줌 (`heroZoom` keyframe)
- 스크롤 리빌 시 카드류 순차 등장 (`nth-child` transition-delay)
- 숫자 카운트업 (`.stat .num`, 10 미만이거나 숫자 아니면 자동 스킵)
- 갤러리 클릭 → 라이트박스 팝업 (좌우 이동, ESC 닫기, `추가사진` 열로 여러 장 지원)
- 인쇄(Ctrl+P → PDF 저장) 전용 CSS
- `prefers-reduced-motion` 대응 — 애니메이션 전부 비활성화 분기 있음, 신규 애니메이션 추가 시 여기도 추가할 것
- URL 정제 함수(`clean`) — 시트 셀에 "⚠️확인:" 같은 메모가 섞여 있어도 실제 URL만 추출

## 코딩 스타일

- 들여쓰기 2칸, 세미콜론 사용
- 색상은 CSS 변수(`--brick`, `--ink` 등)로만 사용, 하드코딩 금지
- 폰트: Paperlogy 단일 패밀리, 다른 폰트 추가 금지 (사용자가 명시적으로 통일 요청함)
- XSS 방지: 시트에서 온 문자열은 반드시 `esc()`로 이스케이프 후 innerHTML에 삽입

## 작업 시 필수 체크리스트

수정 후 배포 전 아래를 스스로 점검할 것 (이전 세션에서 검증 2회 원칙을 지켜왔음):
1. 브라우저에서 문법 에러 없는지 (`node --check`로 `<script>` 내용만 추출해 확인 가능)
2. `images/`, `fonts/` 참조 경로가 실제 파일과 일치하는지
3. 모바일(380px)·PC(1920px) 레이아웃이 깨지지 않는지
4. 시트 로딩 실패 시에도 FALLBACK으로 정상 표시되는지 (백지 방지가 핵심 요구사항)

## 알려진 이력 (반복 실수 방지용)

- 담당자 탭 헤더가 `휴대폰(선택)`/`이메일(선택)`으로 되어있어도 인식하도록 패치되어 있음 — 이 하위호환 제거하지 말 것
- 예전에 하단 티커(마퀴) 기능이 있었으나 "정신사납다"는 이유로 완전히 제거됨 — 재도입 금지
- 예전에 PC 스크롤 스냅이 있었으나 "휠 스크롤이 안 먹힌다"는 이유로 제거됨 — 재도입 금지
- 커버 강조 텍스트가 시트 로딩 전후로 색이 반짝이던 버그 → `**단어**` 마크다운 문법으로 시트에서도 강조색 유지되게 해결함 (`renderCopy`의 정규식 처리)
