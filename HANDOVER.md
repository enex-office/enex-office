# HANDOVER — 에넥스 오피스 홍보 사이트

마지막 갱신: 2026-09-02

이 문서는 다른 계정/세션에서 이 프로젝트를 이어받을 때 가장 먼저 읽어야 하는 문서다.
프로젝트 규칙 자체는 `CLAUDE.md`에 있으니 반드시 같이 읽을 것.

## 지금 상태

- 배포 주소: https://enex-office.github.io/enex-office/ (GitHub Pages, main 브랜치 push 시 자동 재배포, 1~2분 소요)
- 저장소: https://github.com/enex-office/enex-office
- 최근 커밋까지 전부 push 완료된 상태 (2026-08-25 기준 최신)
- 로컬 확인용 정적 서버: `python -m http.server 8080` (레포 루트에서 실행) 로 띄우고 `localhost:8080` 접속.
  ui-inspector 스킬로 `preview_attach`하면 `localhost:5180`에서 핀 찍기(annotation) 방식으로 수정 요청 받을 수 있음.

## 오늘(2026-08-23~25)까지 진행한 작업

1. `#cases` 섹션 "세부 납품 사례" 링크 → 시인성 개선 (밑줄 → 최종적으로 버건디 배경 알약 버튼)
2. 하단 고정 전화바(`.callbar`)·히어로 "전화 견적 문의" 버튼 → `tel:` 전화 연결에서 `#contact`(문의 섹션) 스크롤 이동으로 변경
   - 시트(`links` 탭)의 전화번호 값으로 두 버튼 href를 덮어쓰던 JS(`renderLinks`)도 함께 제거함 — 재도입 금지
3. 모바일에서 하단 전화바가 푸터(회사 주소/연락처)를 가리던 버그 수정 (`footer`에 `padding-bottom` 추가, `max-width:760px` 전용)
4. 문의 섹션(`#contact`)에 버튼 2개 추가:
   - "에넥스 오피스 공식 홈페이지" — 고정 링크 (`https://www.enex.co.kr/office/list.php`)
   - "카탈로그 다운로드" — **시트 `links` 탭의 "카탈로그" 행 URL로 자동 노출**. 지금은 구글폼(정보 입력 후 확인 메시지에서 드라이브 PDF 링크 제공) 방식으로 연결되어 있음. 카탈로그 원본 PDF가 165MB라 GitHub repo에 직접 못 올려서 이 방식을 택함.
5. `#about`(회사소개) 섹션에 신뢰 강조 문구 추가: "가격을 낮췄다고 품질까지 낮추지 않았습니다."
6. 신규 섹션 `#faq` 추가 (`#process`와 `#records` 사이, eyebrow "05 — WE HANDLE THIS"):
   - 헤드라인: "고르고 나면, 그 다음은 저희 일입니다."
   - 포지셔닝 배경: 실사용자(입주민·교수 등)가 아니라 **구매/보고 담당자**가 진짜 고객이라는 통찰에서 나온 카피.
     "제품 품질은 업체마다 거기서 거기라 가격 맞으면 본인 일이 편한 곳을 고른다"는 가설.
   - Q&A 5개: 비교견적 / 실측·도면 / 사진대지·보고서 / 납품 후 A/S 담당자 / 예산-품질 우려
   - 섹션이 하나 늘면서 뒤 섹션들(`#records`~`#contact`)의 배경색(`alt` 클래스) 교차 순서를 재정렬함, eyebrow 번호도 05→10으로 재부여됨

## 2026-09-02 — 전면 개편 시안 작업 (승인 대기)

- 요청: "페이지가 정적·단순해 보여서 움직이는 이미지/영상을 결합하고 싶다 → 전면 개편안 + 샘플 페이지"
- 산출물 (브랜치 `claude/enex-office-setup-mc5otd`, **main 미반영·실사이트 변경 없음**):
  - `docs/REDESIGN_2026-09.md` — 개편안 문서(진단·방향·섹션별 변경·영상 3안·결정 필요 사항·적용 절차)
  - `preview.html` — 샘플 페이지. `index.html`과 같은 경로 구조(`images/`, `fonts/`)라 승인 시 그대로 `index.html`로 교체 가능.
    시트 주소·FALLBACK·파서·렌더 함수·id 전부 기존과 동일. 추가된 것: 커버 영상 슬롯(`HERO_VIDEO` / 시트 links `커버영상`), 사진 슬라이드쇼,
    다크 밴드(#infra·#records), 사업영역 사진 타일, 프로세스 타임라인, 물류 거점 라인, 인증 원형 게이지, 비포/애프터 슬라이더(시트 cases `비포사진` 열, 선택), 상단 진행바, PC 고정 내비
  - 미리보기 Artifact(클로드 코드 채팅에서 전달) — 시트 fetch가 막혀 FALLBACK으로 표시됨. 이미지는 1280px로 축소 임베드된 버전
- 점검 완료: `node --check` 통과, 380/1920 가로 스크롤 없음, 콘솔 에러는 시트 fetch 차단(샌드박스)만
- **같은 날 2차 피드백 반영 (v3)**:
  - 커버 사진 슬라이드쇼 → "아주 좋다" 확정. PC 고정 내비는 스크롤 중 사진을 가려 제거. 갤러리 첫 카드 2칸 확대도 제거
  - #business 사진 타일: 글자가 사진 위에 겹쳐 안 읽힘 → 사진(위)+어두운 글 판(아래) 분리 구조로 변경
  - #infra 거점 표현: 가로 타임라인은 "단계를 거친다"는 인상 → 황간 공장 중심 **방사형 거점 지도(SVG)** + "7거점 · 동시 출고" 문구로 교체
- **⚠️ 발견한 데이터 문제 (index.html에도 그대로 존재, 실사이트는 시트가 정상 로딩되면 영향 없음)**:
  - `FALLBACK.cases`의 사진파일명이 실제 `images/` 파일 내용과 어긋남 — 2026-07-06 images 폴더를 통째로 지우고 재업로드하면서 이름이 섞인 듯.
    실제 내용: case_dorm=스터디부스, case_comm_booth=피트니스, case_comm_gym=스크린골프룸, case_cafeteria=강의대 제품사진(사례 아님). 기숙사·카페테리아 사진은 없음.
    preview.html의 FALLBACK은 사진 내용 기준으로 임시 재매핑(기숙사·카페테리아는 "PHOTO 준비중"). **시트 `cases` 탭의 사진파일명이 실제 사진과 맞는지 사장님 확인 필요.**
  - `FALLBACK.contacts`에 조인진 차장 없음(이종민·김지민만). 시트에는 있을 것으로 추정 — 이 환경은 docs.google.com 접근이 차단되어(curl·WebFetch 모두 EGRESS_BLOCKED) 시트를 직접 읽을 수 없었음.
  - 이 두 FALLBACK은 시트 로딩 실패 시 노출되는 안전망이므로, 시트 현재값을 받아 index.html·preview.html 양쪽 FALLBACK을 동기화해야 함 (CLAUDE.md 원칙)
- **2026-09-02 3차 피드백 (v4)**: 거점 지도에 **서울 본사(서초)** 흰 링 마커 추가 + 안성과 묶어 점선 "수도권" 영역 표기.
  SVG 라벨 좌우정렬은 `text-anchor` 속성이 CSS에 밀리므로 `.tl`/`.tr` 클래스로 처리할 것 (속성으로 쓰면 무시됨)
- **카탈로그 배너가 시안에 없는 이유**: 시트 `links` 탭 "카탈로그" 행에서 URL을 읽어 렌더하는데(renderLinks의 `find("카탈로그")`),
  이 환경은 시트 접근이 막혀 FALLBACK.links를 쓰고 거기엔 카탈로그 행이 없음. 조인진 차장 누락도 같은 원인. **코드 문제 아님**
- **권장 검증 절차**: preview.html을 main에 추가 파일로만 push → `.../preview.html`에서 실제 시트 데이터로 확인 → 그 후 index.html 교체.
  이 방식이면 검증 단계에서 실사이트가 전혀 안 바뀜
- **다음 세션 할 일**: (1) 시트 `contacts`·`cases` 탭 현재 행을 사용자에게 받아 FALLBACK 동기화 (2) 문서 §9 나머지 결정 받기 → `preview.html` 반영 → `index.html` 교체 → CLAUDE.md 갱신 → main push
- 주의: `preview.html`은 시안이므로 main에 올리면 `/preview.html` 주소로 공개됨. 교체 전까지는 브랜치에만 둘 것

## 진행 중 / 미완료

- **카탈로그 구글폼**: 폼 자체는 만들어졌고 시트에도 URL 연결됨. 사용자가 "확인 메시지에 다운로드 링크 안 뜬다"고 해서 저장 안 된 걸로 진단 — 재저장 안내는 했으나 **최종 재확인은 아직 안 됨**. 다음 세션에서 카탈로그 버튼 클릭 → 폼 제출 → 다운로드 링크까지 실제로 나오는지 점검 필요.
- **힉스필드(Higgsfield) AI 영상**: 사용자가 "옛날 사무실 → 새 가구로 바뀌는" 비포/애프터 영상을 히어로 배경 등에 쓰고 싶어함. MCP 서버는 연결 시도까지 진행(`claude mcp add --transport http --scope user higgsfield https://mcp.higgsfield.ai/mcp`), OAuth 인증은 아직 안 됨. 무료 요금제는 워터마크·기능 제한 있어 실사용엔 유료(Starter $19/월~) 필요할 가능성 큼. **오늘은 여기까지만 하고 다음으로 미룸.**
- FAQ 섹션 문항 5개는 초안 — 실제 고객 반응 기반으로 다듬을 여지 있음.

## 지켜야 할 것 (CLAUDE.md 요약 + 이번 작업에서 얻은 추가 원칙)

- 시트(`links`/`copy`/`contacts`/`records`/`cases`) 구조 깨지 않기. 새 링크(카탈로그처럼)를 추가할 땐 코드에서 `find("키워드")`로 시트 항목을 찾아 조건부 렌더링하는 기존 패턴을 그대로 따를 것 — 하드코딩 금지.
- 새 섹션/카피를 추가할 때 사장님 승인 없이 밀어붙이지 말 것. 이번 FAQ 섹션도 카피 방향(A/B/C안) 제시 → 사용자가 B 선택 → 문구 확정 → 로컬 확인 → push 순서로 진행함. 이 워크플로우를 유지할 것.
- push는 항상 사용자 확인 후에만. 로컬(`localhost:8080` 또는 `5180`)에서 문법 에러(`node --check`) + 콘솔 에러(ui-inspector의 `preview_errors`) 확인 후 진행.
- 구글 드라이브/폼 등 팀 자산은 개인 계정(a65339673)보다 팀 계정(enex2095@gmail.com) 사용을 권장함 — 구글캘린더 연동도 이미 이 계정 기준.

## 참고

- 프로젝트 규칙 원본: `CLAUDE.md` (이 저장소 루트, 이번에 처음 git에 커밋됨 — 그 전엔 로컬에만 있었음)
- 사용자는 비개발자(오피스팀, 이종민 대리) — 수정 후 "무엇이 바뀌었는지 + 확인 방법"을 항상 짧게 설명할 것. 장황한 설명보다 간결한 답변을 선호함.
